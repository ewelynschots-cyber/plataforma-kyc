services:
  - type: web
    name: plataforma-kyc
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.app:create_app()
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
